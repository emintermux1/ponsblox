import { LaunchForm } from '@/components/LaunchForm'

export default function LaunchPage() {
  return (
    <main className="mx-auto w-full max-w-[1200px] px-4 py-8 pb-28 lg:pb-8">
      <LaunchForm />
    </main>
  )
}
